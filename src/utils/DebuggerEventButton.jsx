import React from "react";

function DebuggerEventButton() {
  const handleClick = () => {
    //   here event to test
  };

  return <button onClick={handleClick}>Click Me</button>;
}

export default DebuggerEventButton;
