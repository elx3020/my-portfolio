
const HeroContainer = (props) => {

    const { backgroundImage, title, description } = props;



    return (
        <div style={{height:'100vh', backgroundColor:'blue' }}>Hero</div>
    )
}

export default HeroContainer;

